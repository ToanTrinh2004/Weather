import javax.swing.*;
import java.awt.*;

public class LocationNavBar extends JPanel {
    public LocationNavBar(){
        setBounds(0, 50, 1920, 50); // Full width
        setBackground(Color.blue);
        setLayout(null);
    }
}
